<Window x:Class="Civil3DCutSolid.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Civil3DCutSolid.Views"
             mc:Ignorable="d" 
             Height="200" Width="400">
    <StackPanel>
        <Button Content="Select an alignment"
                Padding="2"
                Margin="2"
                Command="{Binding SelectAlignment}"/>

        <Label Content="Step" Padding="2" Margin="2"/>
        <TextBox Text="{Binding Step}" Padding="2" Margin="2"/>

        <Button Content="Select solids"
                Padding="2"
                Margin="2"
                Command="{Binding SelectSolidsCommand}"/>
    </StackPanel>
</Window>
